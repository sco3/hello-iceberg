# Set the root logger to WARN to hide INFO messages
rootLogger.level = warn

# Specifically silence common noisy Spark components
logger.spark.name = org.apache.spark
logger.spark.level = warn

# Optional: Further suppress Hadoop/Jetty if they are still too noisy
logger.hadoop.name = org.apache.hadoop
logger.hadoop.level = error